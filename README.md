# Serverless-To-Do-REST-API-on-AWS
A fully serverless CRUD API built using AWS API Gateway, Lambda, and DynamoDB. Designed with least-privilege IAM, CORS enabled for browser access, and implemented entirely through the AWS Console while remaining Free-Tier safe.
